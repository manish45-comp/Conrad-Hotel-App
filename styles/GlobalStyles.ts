import { StyleSheet } from "react-native";

export const GlobalStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    padding: 20,
  },
  title: {
    fontSize: 32,
    textAlign: "center",
    marginBottom: 50,
  },
  subtitle: {
    fontSize: 18,
    color: "#6B7280",
    fontWeight: "500",
    textAlign: "center",
  },
});
